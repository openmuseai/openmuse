# AppFlowy Workspace Plugin

Logical Plugin `muse.appflowy.workspace`. Pins a DSH cwd (README only), lists AppFlowy folder views via Cloud collab (`muse_workspace_list_views`), and never treats the DSH working directory as the page tree.

See [TECH.zh-CN.md](TECH.zh-CN.md).
