# AppFlowy View Rename Plugin

Hot-pluggable Cordis/DSH Tools for proposing and applying a rename to the AppFlowy View selected by the Host.
The Plugin knows only the public capability contract. It cannot access View IDs, Folder/Collab, approval proofs, or grants;
the generic Muse Plugin Kit resolves Host policy before invoking the `local_write` operation.
