[package]
name = "muse-plugin-facets"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Rust projection of the domain-neutral Muse Plugin Facet v1 contract"

[dependencies]
jsonschema = { version = "0.49.9", default-features = false }
muse-host-bridge-contract = { path = "../../protocol/host-bridge/rust" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"

[dev-dependencies]
serde_json = "1.0.151"
