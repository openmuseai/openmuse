name: muse_table_surface
description: AppFlowy Database/Grid presentation facet for Muse v2.
version: 0.2.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  muse_plugin_facets:
    path: ../muse_plugin_facets
  muse_ui_surface_runtime:
    path: ../muse_ui_surface_runtime

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
