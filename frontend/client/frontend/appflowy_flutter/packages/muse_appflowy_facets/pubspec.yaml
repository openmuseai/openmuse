name: muse_appflowy_facets
description: AppFlowy presentation facets shared by native platform carriers.
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  muse_plugin_facets:
    path: ../muse_plugin_facets
dev_dependencies:
  flutter_test:
    sdk: flutter
