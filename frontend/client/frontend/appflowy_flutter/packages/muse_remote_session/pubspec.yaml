name: muse_remote_session
description: Platform-neutral Flutter client state machine for Muse Remote Runtime.
version: 0.2.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
dependencies:
  muse_plugin_facets:
    path: ../muse_plugin_facets
